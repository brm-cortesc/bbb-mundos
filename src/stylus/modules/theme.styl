.body-lock
  pointer-events none
  header
    position relative
    z-index 99999

.cereza
  background-image $Rojo-grad
  overflow hidden

  a
    color $Rojo[0]

    &:hover
      color $Rojo[1]


  .btn-zoom-in,
  .btn-zoom-out
    color $Rojo[1]


.limon
  background-image $Verde-grad
  overflow hidden
  a
    color $Verde[0]

    &:hover
      color $Verde[1]

  .btn-zoom-in,
  .btn-zoom-out
    color $Verde[1]


.resultados
  overflow hidden
